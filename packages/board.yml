substitutions:
  espidf_version: "5.3.3"
  platformio_version: "6.11.0"

esp32:
  board: $board
  variant: $variant
  flash_size: $flash_size
  framework:
    type: esp-idf
    version: $espidf_version
    platform_version: $platformio_version
